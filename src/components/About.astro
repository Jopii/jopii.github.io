---
// About.astro - Sección de perfil con tema soft/cute
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;

const skills3D = [
  "Blender",
  "Substance Painter",
  "ZBrush",
  "Character Design",
  "Props",
  "Game Assets"
];

const skillsCode = [
  "Python",
  "JavaScript",
  "HTML/CSS",
  "Astro",
  "Git/GitHub"
];

const skillsAI = [
  "PyTorch",
  "OpenCV",
  "Stable Diffusion",
  "AI Generativa"
];
---

<section id="about" class={`py-20 md:py-28 bg-white ${className}`}>
  <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex flex-col md:flex-row items-center gap-10 md:gap-16">
      <!-- Foto de perfil -->
      <div class="flex-shrink-0">
        <div class="relative">
          <img
            src="/img/pfp-1.webp"
            alt="Foto de perfil de Jesús Jódar, artista 3D y desarrollador"
            class="w-40 h-40 md:w-48 md:h-48 rounded-full object-cover border-4 border-[#f472b6]/20 shadow-xl"
            width="192"
            height="192"
          />
          <div class="absolute -bottom-2 -right-2 w-12 h-12 bg-[#f472b6] rounded-full flex items-center justify-center shadow-lg">
            <span class="text-2xl">✨</span>
          </div>
        </div>
      </div>
      
      <!-- Información -->
      <div class="text-center md:text-left flex-1">
        <p class="text-[#f472b6] font-medium mb-2 tracking-wide uppercase text-sm">Hola, soy</p>
        <h2 class="text-3xl md:text-4xl font-bold text-[#2d2a26] mb-2">
          Jesús Jódar
        </h2>
        <p class="text-lg text-[#a78bfa] mb-6">3D Artist & Creative Developer</p>
        
        <div class="space-y-4 text-[#6b6560] text-base md:text-lg leading-relaxed mb-8">
          <p>
            Tengo 24 años y actualmente estudio diseño y desarrollo de videojuegos. 
            Cuento con cuatro años de experiencia como artista 3D junior, creando 
            experiencias visuales para artistas musicales y proyectos personales.
          </p>
          <p>
            Más allá del arte 3D, desarrollo herramientas en <span class="text-[#f472b6] font-medium">Python</span> para 
            automatización de pipelines y exploro el mundo del <span class="text-[#a78bfa] font-medium">Machine Learning</span> 
            para generación y manipulación de assets. Me apasiona la intersección entre 
            creatividad y tecnología.
          </p>
        </div>
        
        <!-- Skills Tags -->
        <div class="space-y-4">
          <div>
            <p class="text-sm text-[#6b6560] mb-2 font-medium flex items-center gap-2">
              <span>🎨</span> Arte 3D
            </p>
            <div class="flex flex-wrap justify-center md:justify-start gap-2">
              {skills3D.map((skill) => (
                <span class="px-3 py-1.5 text-sm text-[#2d2a26] bg-[#f472b6]/10 rounded-full border border-[#f472b6]/20 hover:bg-[#f472b6]/20 transition-colors duration-200">
                  {skill}
                </span>
              ))}
            </div>
          </div>
          
          <div>
            <p class="text-sm text-[#6b6560] mb-2 font-medium flex items-center gap-2">
              <span>💻</span> Desarrollo
            </p>
            <div class="flex flex-wrap justify-center md:justify-start gap-2">
              {skillsCode.map((skill) => (
                <span class="px-3 py-1.5 text-sm text-[#2d2a26] bg-[#a78bfa]/10 rounded-full border border-[#a78bfa]/20 hover:bg-[#a78bfa]/20 transition-colors duration-200">
                  {skill}
                </span>
              ))}
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
